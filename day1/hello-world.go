package main

import "fmt"

func main(){
	fmt.Println("😍 is an emoji")
	fmt.Println(`The main shortcut to open the Mac emoji keyboard (Character Viewer) is Control + Command + Spacebar. Pressing these three keys simultaneously 
	will bring up a searchable panel of emojis and symbols, which can then be clicked to insert into a text field
	`)
}